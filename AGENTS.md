# ForgeX 项目协作规则

## 产品定位

ForgeX 是面向产品经理、需求分析师和初级研发的 AI 软件交付控制面。默认界面使用业务名称、自然语言状态和下一步提示，不把数据库 ID、任务编码、Trace ID 或内部枚举直接暴露给普通用户。技术标识只允许出现在可展开的“技术详情”中。

## 开发规则

- 所有功能先写用户旅程和失败测试，再写最小实现。
- 领域对象、Artifact 和外部 API 必须版本化，并提供可读错误信息。
- Codex 账户凭据只保留在客户设备本地；控制面只保存账户显示名、不可逆指纹、能力和在线状态。
- 不允许 Agent 直接获得生产密码、通用 Shell 或未审批的生产写权限。
- 普通用户页面使用中文友好文案；代码标识符、API 和协议字段使用英文。
- Git 提交信息和代码注释使用简体中文，标识符保持英文。

## 验证命令

```bash
npm test
npm run typecheck
npm run test:coverage
```
